import { Component, OnInit, Input } from '@angular/core';

import Button from './button';

@Component({
  selector: 'itm-button',
  templateUrl: 'button.component.html'
})
export class ItmButtonComponent implements OnInit {
  constructor() { }

  ngOnChanges() { }
}
